{# Auto-generated NDFC MSD Security config data structure for fabric {{ vxlan.fabric.name }} #}
{# SGT is driven by the data model key vxlan.multisite.security.enable_sgt (default false). #}
{% set enable_sgt = vxlan.multisite.security.enable_sgt | default(defaults.vxlan.multisite.security.enable_sgt) | ansible.builtin.bool %}
  ENABLE_SGT: "{{ 'on' if enable_sgt else 'off' }}"
